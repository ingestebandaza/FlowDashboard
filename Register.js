/**
 * FlowRegister Auto.js runner (Dummy)
 * En FlowDashboard >= 1.0.44 la creacion de cuentas utiliza 
 * Sockets nativos en local_adb_server.py (FlowAgent).
 * Este archivo se intercepta y ejecuta el motor Python.
 */

auto();
toast("Iniciando motor de Registro por Sockets...");
sleep(2000);
exit();